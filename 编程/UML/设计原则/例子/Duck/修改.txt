以下内容需要修改:
1. Aircraft和Duck都能被shoot, 你设计一个interface叫shootable, 包含一个方法shot()表示被shoot后的反应.Aircraft和Duck实现这个接口. 把原来的die()和destory()换成这个shot()
2. 为了展示我对工厂模式的理解, 在Duck.md中我已经用了简单工厂, 现在我需要在Aircraft中用正常的工厂模式, 也就是还需要BoeingFactory和ApacheFactory. 在Person.md中,删除这些Factory,直接不用了.
3. 在Peron.md中,我需要展示装饰器模式.具体构建为男孩女孩猎人, 具体装饰器为各种衣服(你再多添加一点衣服)

以下内容需要修改:
1. 删除aircraft的takeoff()方法
2. 删除person的hideinhouse方法
3. aircraft和其observer的关系设置成关联就行, weather和其observer也是同理

以下内容需要修改:
1. 把衣服的类型改为HuntingSuit
CasualSuit
DownJacket
Boots
Jeans
Shirt
Sneaker
LongHair
ShortHair
2. 我还需要添加个人飞行翼和飞行摩托，背上或骑上就可以飞行。

以下内容需要修改:
1. 每个人都要穿环境适应服：环境适应服不仅能防雨，还能防寒、防火、提供氧气（用于高空或水下）。这是用设计模式中的组合来做.环境适应服也是一个weatherObserver, 当环境改变时, 会切换提供的功能. 
2. 每个人也是weatherObserver, 当环境改变时,需要能够切换衣服.人物能换衣服的这个功能是可以动态调整的, 在运行时需要能够禁止/启用
3. 添加两类衣服: 雨衣, 防水鞋
4. 鸭子也是weatherObserver

搞错了一个点, 环境适应服其实是一个类似控制器的东西, 它是环境的observer, 当环境改变, 它切换在场所有人物的衣服. 这样person就不用是weatherObserver了.



以下内容需要修改:
1. 在天气变换这个场景中, 需要有weather这个类, weather一开始可以默认设为晴朗, 然后切换为暴风雨. 请注意在我的设计中, weather才是能够添加Observer的, 而EnvironmentAdaptiveSuitController只是weather的observer而已.所以正确的逻辑是, weather先把duck和EnvironmentAdaptiveSuitController注册为它的observer, weather改变, 几个observer分别作出反应
2. 移除duck的seek'shelter方法, 就直接写在onweatherchange()方法里就行
3. duck的quackBehavior请你同步更新类图中的和代码中的

以下内容需要修改:
1. duck的quackBehavior在当前基础上还要加上quack(替换quackNormal),squick, muteQuack(替换QuackSilent)
2. 关于淘气小精灵禁止换装的这个功能, 可以这么设计. 添加一个类Magic, Magic的observer是Person. 当小精灵对某人施法后, 会创建Magic, Magic添加这个人作为Observer.这个人的autoSwitchEnabled会被设成false. Magic自己计时20s, 当20s到时, 通知它的Observer, 打印"魔法解除", 将autoSwitchEnabled设为true