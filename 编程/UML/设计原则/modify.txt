以下内容需要修改:
1. 场景中, 有三种移动方式,分别是地上走, 水上游, 天上飞. 这个也可以用接口的形式做.person,duck,aircraft都要实现该接口
2. plane需要实现flybehavior这个接口, 且有performFly这个方法,flyBehavior这个属性(跟duck类似) 
3. 关于移动方式, Duck和Plane需要能中途改变移动方式, 因为在场景一中:MallardDuck需要从水上飞向空中,这涉及到移动方式的改变;场景二中:一架Boeing飞机和一架Apache飞机依次出现，并起飞在空中飞行,也需要移动方式的改变.


以下内容需要修改:
1. 在MoveBehavior中, 添加两种fly, 分别是给boeing的FlyWithPropulsion,以及给apache的FlyWithPropeller
2. 修改performMove的名字, 根据对应的对象, 改成performFly,performSwim,performWalk
3. Aircraft类需要添加起飞方法,这相当于一种封装


以下内容需要修改:
1. 添加Weather这个类,这个类相当于一个enumeration,有Cold和Hot两种.
2. SimulationControllor有weather这个属性, 并且可以setWeather,runScene()

以下内容需要修改:
1. 由于场景中不同对象只出现了一次, 所以可以把对象名字就改成对应类名即可
2. 修改shotDuck.md中的类图, 将name这个属性加给Aircraft,Duck,Person.
3. 在场景描述中,"出现"意味着display(),对于Hunter和Boy,Girl来说,display()方法需要展示它们的穿着
4. 修改shotDuck.md中的类图, 不要用ClothingBehavior做接口,用Clothing类做继承,三个子类:男孩冬装,女孩冬装,成年人夏装

以下内容需要修改:
1. 猎人shoot,将打印猎人shoot这件事.你需要在scene中多添加一条duck.die()以体现鸭子死了.你需要确保Hunter的shoot和duck的die都能返回字符串
2. 把boy.setClothing(std::make_unique<BoyWinterClothing>());boy.setMoveBehavior(std::make_unique<WalkOnLand>());这类语句直接写到类的初始化中,不要在scene中添加.类似的, hunter和girl的相应部分也要修改
3. 关于performXX,你需要修改逻辑为:先在内部调整moveBehavior,再当场执行该moveBehaivor
4. 关于两架飞机, 登场后需要先performStopOnGround,然后takeoff,然后perform它们的飞行模式.takeoff也需要在scene中添加,返回一个字符串

有一个大改动:
修改shotDuck.md中的类图, 将所有MoveBehavior改为FlyBehavior,并删除它的子类:Walk,Swim和Stop.取消person与MoveBehavior的关联
把person中的Movebehavior属性删掉,删掉Aircraft的方法:perfromStopOnGround.
QuackBehavior需要添加<<interface>>标签

相应的修改main.cpp中的内容