适用于本地有vpn的情况


仅GitHub走Clash代理
```bash
git config --global 'http."https://github.com".proxy' http://127.0.0.1:7897
```
- 7897: clash的默认端口

可通过以下命令查看配置
```bash
git config --global --list
```

验证配置是否生效
```bash
git config --get-urlmatch http.proxy https://github.com
```
输出`http://127.0.0.1:7897`即生效