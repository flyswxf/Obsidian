## 核心思想
多头注意力机制（Multi-Head Attention）是 Transformer 的核心组件之一。它的主要思想是：不只做一次注意力计算，而是将 Query、Key、Value 映射到多个不同的子空间中（拆分为多个 "头"），在各个子空间里并行计算注意力，最后再将结果拼接（Concat）并进行一次线性映射。

这样做的目的是**让模型能够在不同的表示子空间里，关注到输入序列的不同特征（如语法、语义、上下文逻辑等）**。

公式表示：
$$ \text{MultiHead}(Q, K, V) = \text{Concat}(\text{head}_1, \dots, \text{head}_h)W^O $$
$$ \text{where } \text{head}_i = \text{Attention}(QW_i^Q, KW_i^K, VW_i^V) $$

## PyTorch 使用

在实际开发中，通常不需要手写 Multi-Head Attention，PyTorch 提供了高度优化的内置模块 `nn.MultiheadAttention`。

```python
import torch
import torch.nn as nn

d_model = 512  # 输入和输出的维度
num_heads = 8  # 头的数量 (d_model 必须能被 num_heads 整除)

# 初始化多头注意力层
# batch_first=True 表示输入张量的形状为 (batch_size, seq_len, d_model)
mha = nn.MultiheadAttention(embed_dim=d_model, num_heads=num_heads, batch_first=True)

# 模拟输入 (Query, Key, Value 相同，即自注意力)
batch_size, seq_len = 2, 10
query = torch.randn(batch_size, seq_len, d_model)
key = torch.randn(batch_size, seq_len, d_model)
value = torch.randn(batch_size, seq_len, d_model)

# 前向传播
# 返回值：
# attn_output: 经过多头注意力计算后的输出
# attn_weights: 注意力权重矩阵
attn_output, attn_weights = mha(query, key, value)

print("输出形状:", attn_output.shape)    # [2, 10, 512]
print("权重形状:", attn_weights.shape)   # [2, 10, 10]
```

## 关键要点说明
- **维度拆分与拼接**：在实现上，通常并不是实例化 $h$ 个独立的线性层，而是用一个大的线性层输出后，利用 `reshape/view` 将 `d_model` 拆分为 `num_heads * d_k`。计算完成后，再 `view` 回 `d_model`。这极大提高了并行计算效率。
- **信息多样性**：类似于卷积神经网络 (CNN) 中的多个卷积核（通道），不同的注意力头能够捕捉输入序列中不同维度的关联模式。
- **必须整除**：为了方便拆分和拼接，输入维度 `d_model` 必须能够被头数 `num_heads` 整除，每个头的维度通常为 $d_k = d_{model} / h$。
