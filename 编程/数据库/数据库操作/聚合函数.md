接收一个RELATION

- avg()
	- 忽略null
	- 配合`group by`进行分组求平均
	- `select dept_name, avg (salary) as avg_salary from instructor group by dept_name`
	  ![[Pasted image 20250308144357.png]] ^792894
- max()
- min()
- count()
	- **忽略null, 不去重**
	- 使用前缀**distinct**去重, 如`select count(distinct ID) from course`
	- 特例: **count(\*)统计表中所有记录, 不忽略null**, 如`selcet count(*) from course`