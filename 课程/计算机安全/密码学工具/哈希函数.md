密码学哈希函数（又称消息摘要函数）是一种将**任意长度**的输入数据映射为**固定长度**输出的数学函数：
$$ h = H(M) $$

- 输入 $M$：任意长度的消息
- 输出 $h$：固定长度的哈希值（摘要），如 SHA-256 输出 256 bits

---

## 密码学哈希函数的安全要求

| 属性                                          | 描述                                                                 |
| --------------------------------------------- | -------------------------------------------------------------------- |
| **抗原像性 (Preimage Resistance)**            | 给定 $h$，在计算上不可行找到任意 $M$ 使得 $H(M) = h$。即 "单向性"    |
| **抗第二原像性 (Second Preimage Resistance)** | 给定 $M_1$，在计算上不可行找到 $M_2 \neq M_1$ 使得 $H(M_2) = H(M_1)$ |
| **抗碰撞性 (Collision Resistance)**           | 在计算上不可行找到任意 $M_1 \neq M_2$ 使得 $H(M_1) = H(M_2)$         |

> **碰撞攻击总是可能的**（鸽巢原理），但找到碰撞在计算上应是不可行的。

---

## 常用哈希算法

| 算法           | 输出长度 (bits) | 安全状态                                         |
| -------------- | :-------------: | ------------------------------------------------ |
| MD5            |       128       | **已破解**，不应用于安全场景                     |
| SHA-1          |       160       | **已破解**，已实现实际碰撞攻击 (SHAttered, 2017) |
| SHA-256        |       256       | 当前安全，广泛使用                               |
| SHA-384        |       384       | 当前安全                                         |
| SHA-512        |       512       | 当前安全                                         |
| SHA-3 (Keccak) |      可变       | 最新标准 (NIST 2015)                             |
| BLAKE3         |      可变       | 现代高性能哈希                                   |

---

## 哈希函数的应用

### 1. 数字签名
对消息哈希值签名，而非对完整消息签名。大幅提升效率并增强安全性。

### 2. 消息认证 (HMAC)
$$ HMAC(K, M) = H((K \oplus opad) \parallel H((K \oplus ipad) \parallel M)) $$
结合密钥和哈希函数，构造[[消息认证码(MAC)#1. 基于哈希的 MAC (HMAC)|消息认证码]]。

### 3. 密码存储
存储密码的哈希值（加盐），而非明文密码。
- **加盐 (Salting)**：为每个用户生成[[随机与伪随机数|随机]]盐值 $salt$，存储 $H(password \parallel salt)$
- 防止[[暴力破解攻击#3. 彩虹表攻击 (Rainbow Table Attack)|彩虹表攻击]]和相同密码产生相同的哈希值

### 4. 数据完整性校验
- 文件下载校验（校验和 checksum）
- 区块链（Merkle 树依赖哈希链接）
- 版本控制系统 (Git 使用 SHA-1 进行内容寻址)

### 5. 伪随机数生成
哈希函数可作为 [[随机与伪随机数|CSPRNG]] 的构建块，例如基于 HMAC 的 DRBG。

---

## 雪崩效应 (Avalanche Effect)

密码学哈希函数应具备的特性：输入中**任何微小的变化**（哪怕是一个比特的翻转）都会导致输出中**约 50% 的比特位**发生变化。这确保了哈希值的不可预测性。

---

## 生日攻击 (Birthday Attack)

![[assets/birthday_attack.svg]]

生日攻击是一种针对哈希函数**抗碰撞性**的密码学攻击手段，其核心数学基础是概率论中的**生日悖论 (Birthday Paradox)**。

对于输出长度为 $n$ 位的哈希函数，其哈希值空间大小为 $N = 2^n$：

- **暴力破解原像 (Preimage Attack)**：试图寻找特定哈希值 $h$ 的输入 $M$。单次命中的概率为 $1/2^n$，预期需要尝试 $2^n$ 次。
- **生日攻击 (Collision Attack)**：寻找**任意**两个不同的输入 $M_1 \neq M_2$，使得 $H(M_1) = H(M_2)$。这就如同在一个房间里寻找任意两个生日相同的人，我们不需要指定具体是哪一天，只需要两人碰巧在同一天即可。随着样本量增加，配对组合数呈二次方增长。

根据概率论推导，当尝试次数达到约 $\sqrt{N} = 2^{n/2}$ 时，找到碰撞的概率将超过 50%。

因此，哈希函数的**碰撞安全性**折半：
- SHA-256 的抗原像安全性是 **256 位**。
- 但 SHA-256 的抗碰撞安全性实际上只有 **128 位**。
这直接决定了我们在设计哈希函数或选择安全参数时，输出长度必须至少是我们期望的安全级别的**两倍**。
