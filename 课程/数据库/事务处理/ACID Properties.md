- #### 原子性:
	- 事务的所有对数据库的操作**要么全部完成, 要么一个都不做**
- #### 一致性:
	- 如果事务​**​成功提交(Commit)​**​，那么事务执行完成后，数据库必须​**​重新处于完全一致的状态​**​。所有暂时性不一致都必须被解决，所有显式和隐式的完整性约束都必须被重新满足。
- #### 独立性:
	- **​**​确保事务执行过程中的暂时不一致是临时的、不会被外部看到。
- #### 持久性:
	- 成功执行的事务对数据库的修改不会因其他错误而消失