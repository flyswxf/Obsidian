
给定下面文法及语义动作，输入 `abc` 时对应的输出是什么？输入是 `dc` 时对应的输出是什么？请分别写出语义动作执行过程。

$$
\begin{aligned}
S &\rightarrow aA \{ \text{putchar}('a'); \} B \mid AB \\
A &\rightarrow bA \{ \text{putchar}('b'); \} \mid \epsilon \{ \text{putchar}('e'); \} \\
B &\rightarrow c \{ \text{putchar}('c'); \} \mid dA \{ \text{putchar}('d'); \} B
\end{aligned}
$$

## 问题解答

在语法制导翻译方案（SDT）中，语义动作通常在深度优先遍历语法树（或自顶向下分析）时，按照从左到右的顺序执行。即当分析器推导或匹配完动作左侧的文法符号后，立刻执行该语义动作。

### 1. 输入 `abc` 的执行过程与输出
对于输入 `abc`，分析器从起始符号 $S$ 开始推导：

1. 输入以 `a` 开头，选择产生式 $S \rightarrow aA \{ \text{putchar}('a'); \} B$。
   - 匹配终结符 `a`。
2. 此时剩余输入为 `bc`，遇到非终结符 $A$，选择产生式 $A \rightarrow bA \{ \text{putchar}('b'); \}$。
   - 匹配终结符 `b`。
3. 此时剩余输入为 `c`，再次遇到非终结符 $A$，由于输入不以 `b` 开头，选择产生式 $A \rightarrow \epsilon \{ \text{putchar}('e'); \}$。
   - 匹配空串 $\epsilon$。
   - **执行动作 1**：执行 $\{ \text{putchar}('e'); \}$，输出 `e`。
4. 当前 $A \rightarrow \epsilon \{ \text{putchar}('e'); \}$ 规约完成，回退到上一层 $A \rightarrow bA \{ \text{putchar}('b'); \}$。
   - **执行动作 2**：执行 $\{ \text{putchar}('b'); \}$，输出 `b`。
5. 当前 $A$ 规约完成，回退到第一层 $S \rightarrow aA \{ \text{putchar}('a'); \} B$。
   - **执行动作 3**：执行 $\{ \text{putchar}('a'); \}$，输出 `a`。
6. 遇到非终结符 $B$，此时剩余输入为 `c`，选择产生式 $B \rightarrow c \{ \text{putchar}('c'); \}$。
   - 匹配终结符 `c`。
   - **执行动作 4**：执行 $\{ \text{putchar}('c'); \}$，输出 `c`。

**最终输出结果：** `ebac`

### 2. 输入 `dc` 的执行过程与输出

**语法推导与动作执行过程：**

对于输入 `dc`，分析器从起始符号 $S$ 开始推导：

1. 输入以 `d` 开头，选择产生式 $S \rightarrow AB$。
2. 遇到非终结符 $A$，由于输入不以 `b` 开头，选择产生式 $A \rightarrow \epsilon \{ \text{putchar}('e'); \}$。
   - 匹配空串 $\epsilon$。
   - **执行动作 1**：执行 $\{ \text{putchar}('e'); \}$，输出 `e`。
3. 遇到非终结符 $B$，此时剩余输入为 `dc`，选择产生式 $B \rightarrow dA \{ \text{putchar}('d'); \} B$。
   - 匹配终结符 `d`。
4. 此时剩余输入为 `c`，遇到非终结符 $A$，由于输入不以 `b` 开头，选择产生式 $A \rightarrow \epsilon \{ \text{putchar}('e'); \}$。
   - 匹配空串 $\epsilon$。
   - **执行动作 2**：执行 $\{ \text{putchar}('e'); \}$，输出 `e`。
5. 回退到上一层 $B \rightarrow dA \{ \text{putchar}('d'); \} B$。
   - **执行动作 3**：执行 $\{ \text{putchar}('d'); \}$，输出 `d`。
6. 遇到非终结符 $B$，此时剩余输入为 `c`，选择产生式 $B \rightarrow c \{ \text{putchar}('c'); \}$。
   - 匹配终结符 `c`。
   - **执行动作 4**：执行 $\{ \text{putchar}('c'); \}$，输出 `c`。

<<<<<<< HEAD
**最终输出结果：** `eedc`
=======
<<<<<<< HEAD
**最终输出结果：** `eedc`

---

> [!warning]- 作业提交格式
> 以下为提交版的简洁格式，省略了 SDT 理论说明，直接给出推导过程。
>
> ### 1. 输入 `abc` 的执行过程与输出
>
> 对于输入 `abc`，分析器从起始符号 $S$ 开始推导：
>
> 1. 输入以 `a` 开头，选择产生式 $S \rightarrow aA \{ \text{putchar}('a'); \} B$。
> 	- 匹配终结符 `a`。
> 2. 此时剩余输入为 `bc`，遇到非终结符 $A$，选择产生式 $A \rightarrow bA \{ \text{putchar}('b'); \}$。
> 	- 匹配终结符 `b`。
> 3. 此时剩余输入为 `c`，再次遇到非终结符 $A$，由于输入不以 `b` 开头，选择产生式 $A \rightarrow \epsilon \{ \text{putchar}('e'); \}$。
> 	- 匹配空串 $\epsilon$。
> 	- 执行动作 1：执行 $\{ \text{putchar}('e'); \}$，输出 `e`。
> 4. 当前 $A \rightarrow \epsilon \{ \text{putchar}('e'); \}$ 规约完成，回退到上一层 $A \rightarrow bA \{ \text{putchar}('b'); \}$。
> 	- 执行动作 2：执行 $\{ \text{putchar}('b'); \}$，输出 `b`。
> 5. 当前 $A$ 规约完成，回退到第一层 $S \rightarrow aA \{ \text{putchar}('a'); \} B$。
> 	- 执行动作 3：执行 $\{ \text{putchar}('a'); \}$，输出 `a`。
> 6. 遇到非终结符 $B$，此时剩余输入为 `c`，选择产生式 $B \rightarrow c \{ \text{putchar}('c'); \}$。
> 	- 匹配终结符 `c`。
> 	- 执行动作 4：执行 $\{ \text{putchar}('c'); \}$，输出 `c`。
>
> **最终输出结果：** `ebac`
>
> ### 2. 输入 `dc` 的执行过程与输出
>
> 对于输入 `dc`，分析器从起始符号 $S$ 开始推导：
>
> 1. 输入以 `d` 开头，选择产生式 $S \rightarrow AB$。
> 2. 遇到非终结符 $A$，由于输入不以 `b` 开头，选择产生式 $A \rightarrow \epsilon \{ \text{putchar}('e'); \}$。
> 	- 匹配空串 $\epsilon$。
> 	- 执行动作 1：执行 $\{ \text{putchar}('e'); \}$，输出 `e`。
> 3. 遇到非终结符 $B$，此时剩余输入为 `dc`，选择产生式 $B \rightarrow dA \{ \text{putchar}('d'); \} B$。
> 	- 匹配终结符 `d`。
> 4. 此时剩余输入为 `c`，遇到非终结符 $A$，由于输入不以 `b` 开头，选择产生式 $A \rightarrow \epsilon \{ \text{putchar}('e'); \}$。
> 	- 匹配空串 $\epsilon$。
> 	- 执行动作 2：执行 $\{ \text{putchar}('e'); \}$，输出 `e`。
> 5. 回退到上一层 $B \rightarrow dA \{ \text{putchar}('d'); \} B$。
> 	- 执行动作 3：执行 $\{ \text{putchar}('d'); \}$，输出 `d`。
> 6. 遇到非终结符 $B$，此时剩余输入为 `c`，选择产生式 $B \rightarrow c \{ \text{putchar}('c'); \}$。
> 	- 匹配终结符 `c`。
> 	- 执行动作 4：执行 $\{ \text{putchar}('c'); \}$，输出 `c`。
>
> **最终输出结果：** `eedc`
=======
**最终输出结果：** `eedc`
>>>>>>> f81516b86c4e8635008168039021ce6d584b6c55
>>>>>>> 3c428a2 (chore: 初始化仓库，清理目录结构，合并编译原理作业)
