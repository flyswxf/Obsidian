---
alwaysApply: false
description: 当需要为笔记绘制图表（如流程图、架构图）或意图使用 mermaid 时触发，规范 drawio 绘图及文件存储。
---
1. 统一使用 **drawio** 进行绘图，不要使用 mermaid。
2. 必须将生成的图表导出为 **SVG 格式**。
3. 将生成的图表文件（.svg）以及 drawio 源文件（.drawio）统一放置在相关笔记同级目录下的 `assets` 文件夹中。
